# 技術棧提醒

## 前端
React 19 + Vite 8 + TypeScript，版本較新，API 或慣例可能跟訓練資料中的舊版本不同。寫程式前如果不確定某個 API 是否還是目前寫法，先查 `node_modules/react` / `node_modules/vite` 底下的型別定義或官方文件，不要單憑記憶假設。

## 後端與部署（已定案）
- 後端語言：**Python**（使用者想順便練習 Python，非效能考量）
- 後端框架：**FastAPI**
- 後端容器化：**Docker**
- 後端部署：**Google Cloud Run**
- 前端部署：**Vercel**
- 資料庫：**Neon**（Serverless Postgres，已註冊帳號）

這些是使用者主動選定的方向，之後涉及後端／部署的討論或程式碼都以這個組合為準，不要擅自換成其他框架或平台。

## 資料庫架構：概念層設計（已定案）

- 每個生活紀錄類別（記帳、飲食、身體紀錄、運動、日記…）各自一個 table，彼此獨立
- 「總覽」首頁的加總數字（例如本月記帳總額、今日飲食熱量）**採即時聚合（read-time aggregation），不維護額外的 daily 加總表**
- 原因：維護一份寫入時就算好的 daily 表，代表每個類別 table 新增/修改/刪除紀錄時都要記得同步更新對應那天的加總，容易漏掉、造成資料不一致——這跟史萊姆能力值「不用累加計數器、即時 SUM」是同一個原則。個人使用的資料量下，即時 SUM（搭配日期欄位索引）效能完全足夠，不需要為了不存在的效能問題引入這個複雜度
- 也考慮過用 Postgres trigger 讓 daily 表自動同步（trigger 能解決「同步」問題），但決定不用：邏輯會藏在資料庫層、跟想練習的 Python 是兩回事、migration 管理也更麻煩，而且沒解決「一開始為什麼需要 daily 表」這個根本問題
- 之後如果實際量測到查詢效能有問題，再考慮加 daily 表或 trigger，屆時是效能優化決定，不是現在要預先做的事

## 行動裝置化（方向，尚未定案）
先做網頁版（React/Vite），之後若要 APP 化，傾向用 **Capacitor** 包現有網頁程式碼（可沿用大部分現有 React 程式碼），而不是重寫成 React Native。主要原因：之後若要串 iPhone HealthKit 等原生功能，PWA／一般手機網頁完全無法存取（iOS 上所有瀏覽器都被迫使用 WebKit，這是 OS 層級限制，跟用哪個瀏覽器或是否裝成 PWA 無關），需要原生殼 + 對應 plugin 才能拿到，Capacitor 是相對代價較低的路徑。
